# MedVision AI Frontend

A production-ready React, TypeScript, Vite, Tailwind CSS, and Framer Motion frontend for chest X-ray analysis.

## Run locally

1. Install Node.js and npm.
2. From this folder, run:
   - npm install
   - npm run dev

The app expects the backend API to be available at http://localhost:8000/predict.
